export type ImportedQuestion = {
  id: string;
  descriptor_code: string;
  title: string;
  text: string;
  command: string;
  descriptor: string;
  answer: string;
  answer_item: string;
  options: string;
}